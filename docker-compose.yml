services:
  db:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - flightscan
    healthcheck:
      test: ["CMD", "mysql", "-h", "127.0.0.1", "-u", "root", "-p${MYSQL_ROOT_PASSWORD}", "${MYSQL_DATABASE}", "-e", "SELECT 1"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 15s

  backend:
    build:
      context: ./FlightScan-be
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy
    environment:
      - ConnectionStrings__DefaultConnection=${DB_CONNECTION}
      - Jwt__Key=${JWT_KEY}
      - Jwt__Issuer=${JWT_ISSUER}
      - Jwt__ExpirationHours=${JWT_EXPIRATION_HOURS}
      - Seed__AdminUsername=${SEED_ADMIN_USERNAME}
      - Seed__AdminPassword=${SEED_ADMIN_PASSWORD}
      - ASPNETCORE_ENVIRONMENT=Production
    networks:
      - flightscan

  frontend:
    build:
      context: ./FlightScan-fe
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - flightscan

networks:
  flightscan:

volumes:
  mysql_data:
